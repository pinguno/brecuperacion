name: Docker Image CI

on:
  pull_request:
    types: [closed]

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.r4f4hc8 }}
          password: ${{ secrets.dckr_pat_Ssw9h73qS_AkptM5jG2hUqufbMU }}
      -
        name: Build and push
        uses: docker/build-push-action@v3
        with:
          push: true
          tags: r4f4hc8/imagen3:latest , r4f4hc8/imagen3:${{github.event.pull_request.title}}
